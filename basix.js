// Open the JavaScript console to debug info, etc.
Ctrl+Shift+J

// Array
array = ["a", "b", "c"];
var a = Array("d", "e", "f");

// Add to array
array.push("xyz")

// Remove from Array
array.pop("xyz")

// Get index of array element
var pos = array.indexOf("a")

// Get random element from array
var randElement = array[Math.floor(Math.random()*array.length)]

// Output
document.write(array)
console.log(array)
